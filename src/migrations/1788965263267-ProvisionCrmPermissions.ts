import { MigrationInterface, QueryRunner } from 'typeorm';
import { CRM_PERMISSIONS } from '../crm/crm.constants';
export class ProvisionCrmPermissions1788965263267 implements MigrationInterface { name='ProvisionCrmPermissions1788965263267'; async up(q:QueryRunner){ const roles=await q.query(`SELECT "id" FROM "IDENTITY_ROLES" WHERE "name"=$1 ORDER BY "created_at" LIMIT 1`,['owner']); const role=roles[0] ?? (await q.query(`INSERT INTO "IDENTITY_ROLES" ("name","description","is_active") VALUES ($1,$2,true) RETURNING "id"`,['owner','Owner role']))[0]; for(const [name,description,action] of CRM_PERMISSIONS){const rows=await q.query(`SELECT "id" FROM "IDENTITY_PERMISSIONS" WHERE "resource"=$1 AND "action"=$2`,['crm',action]); const permission=rows[0] ?? (await q.query(`INSERT INTO "IDENTITY_PERMISSIONS" ("name","description","resource","action","is_active") VALUES ($1,$2,$3,$4,true) RETURNING "id"`,[name,description,'crm',action]))[0]; await q.query(`INSERT INTO "IDENTITY_ROLE_PERMISSIONS" ("role_id","permission_id") SELECT $1,$2 WHERE NOT EXISTS (SELECT 1 FROM "IDENTITY_ROLE_PERMISSIONS" WHERE "role_id"=$1 AND "permission_id"=$2)`,[role.id,permission.id]);}}
 async down(q:QueryRunner){for(const [, ,action] of CRM_PERMISSIONS){await q.query(`DELETE FROM "IDENTITY_ROLE_PERMISSIONS" rp USING "IDENTITY_ROLES" r,"IDENTITY_PERMISSIONS" p WHERE rp."role_id"=r."id" AND rp."permission_id"=p."id" AND r."name"=$1 AND p."resource"=$2 AND p."action"=$3`,['owner','crm',action]); await q.query(`DELETE FROM "IDENTITY_PERMISSIONS" WHERE "resource"=$1 AND "action"=$2`,['crm',action]);}} }